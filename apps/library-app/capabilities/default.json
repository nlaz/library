{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core events + file-picker dialog",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
