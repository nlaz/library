Page images are now a bounded cache — the app keeps up to 2 GB and re-creates the rest from your files as you read. Upgrading reclaims several GB on first launch; your documents and notes are untouched. Change or remove the limit in Settings → Storage.
