Search results no longer go blank. Page images are re-created from your files as you read, and a preview that had to wait for one used to give up without saying so — it now waits, asks again, and tells you if a page genuinely can't be recreated. The page-image limit also doubles to 4 GB; change or remove it in Settings → Storage.
