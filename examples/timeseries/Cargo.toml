[package]
name = "timeseries"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
fold = { path = "../../fold" }
serde = { version = "1", features = ["derive"] }
